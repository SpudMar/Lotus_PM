{
  "name": "lotus-pm-infrastructure",
  "version": "0.1.0",
  "private": true,
  "description": "Lotus PM — AWS CDK infrastructure (ap-southeast-2)",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "diff:staging": "cdk diff --all --context environment=staging",
    "deploy:staging": "cdk deploy --all --context environment=staging --require-approval never",
    "diff:production": "cdk diff --all --context environment=production",
    "deploy:production": "cdk deploy --all --context environment=production"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.239.0",
    "constructs": "^10.5.1",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "@types/node": "^20",
    "aws-cdk": "^2.1106.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
